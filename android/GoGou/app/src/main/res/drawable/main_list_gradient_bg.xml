<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--<gradient
        android:startColor="@color/snow"
        android:centerColor="@color/floralwhite"
        android:endColor="@color/snow"
        android:angle="270"/>-->

    <!-- view background color -->
    <solid android:color="@color/white" >
    </solid>

    <!-- view border color and width -->
    <stroke
        android:width="@dimen/list_item_border_width"
        android:color="@color/silver" >
    </stroke>

    <!-- Here is the corner radius -->
    <corners android:radius="@dimen/list_item_corner_radius" >
    </corners>
</shape>